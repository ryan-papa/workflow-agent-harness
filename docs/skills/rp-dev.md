---
description: '[7] 개발. 태스크별 반복 사이클. 통합 브랜치에서 태스크 단위 구현·테스트'
argument-hint: '[태스크 ID 또는 범위]'
---

# rp-dev

태스크별 개발 사이클.

## 트리거

- 태스크 분해 완료 후
- `/rp-dev` 명령

## 절차 (태스크당)

1. 통합 브랜치에서 태스크 브랜치 생성
2. Dev Agent에 1태스크만 위임
3. 빌드 + 테스트 직접 실행 검증 (에이전트 보고만 신뢰 금지)
4. 유닛 테스트 작성 + 실행
5. 통합 브랜치로 머지 + `git log` 검증
6. 검증 통과 시 다음 태스크 자동 진행

## 절대 규칙

- **에이전트당 1태스크만 위임**
- 테스트 코드 없이 커밋/머지 **금지**
- 테스트·빌드 미통과 시 다음 태스크 진행 **금지**
- "급해서", "간단해서" 등 어떤 이유로도 예외 **불가**

## 테스트 규칙

- 비즈니스 로직·DB 접근 중심 (trivial 코드 제외)
- 프론트엔드: 빌드 통과 + 컴포넌트 렌더링 테스트
- 실패 시 Dev Agent 자동 수정 (최대 3회)

## 태스크 완료 체크

- [ ] 테스트 코드 작성됨
- [ ] 테스트 전체 통과
- [ ] 빌드 성공
- [ ] 통합 브랜치 머지 + git log 검증

## 스펙 변경 시

개발 중 스펙 변경 발생 시: PRD 문서 업데이트 → 변경 부분 기획/엔지니어링 리뷰 → 코드 작업. 문서 없이 코드 수정 금지. → [`../harness-dev.md` §스펙 변경 시 플로우](../harness-dev.md)

## 완료 조건

- 전체 태스크 완료

## ▶ 자동 전환

전체 태스크 완료 즉시 `✓ [7] 개발 완료` 출력 후 **`/rp-qa` 자동 진입**.

→ 개발 상세: [`../harness-dev.md`](../harness-dev.md)
