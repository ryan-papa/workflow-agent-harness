# Claude Meta Review r1 — rp-ship PR base 자동 결정

- 실행: 서브에이전트 (general-purpose)
- 대상 PRD: `docs/prd/harness-ship-auto-base/prd.md`
- 영향 파일 범위: `docs/skills/rp-ship.md` · `docs/harness-ship.md` · `CLAUDE.md`
- 근거 회고: `repositories/museum-finder/docs/research/retro-T23.md`

## 점수

| 항목 | 점수 | 근거 |
|------|:---:|------|
| 1. 정확성 (변경 이유 근거성·설계 타당성) | 9 | 회고 I1·I2 · 개선 제안 #1 로 근거 명확. tasks.md 라인 5/137/144/146 실존 확인 — "통합 브랜치: `feat/mvp-v1`" 패턴이 실제로 존재해 감지 동작이 첫 대상 프로젝트에서 즉시 유효. 감지 실패 시 폴백(default branch = 현재 동작) 명시 — regression 리스크 낮음 |
| 2. 설계/구조 (영향 파일 정확성·하네스 일관성) | 8 | 3개 파일 분담 적절. `rp-ship.md`(실행 로직) · `harness-ship.md`(흐름도/표) · `CLAUDE.md`(절대 규칙 1줄) 로 책임 분리. 단 **기존 절대 규칙 "feat 브랜치 직행 배포 금지"(CLAUDE.md L125)와 신규 규칙의 의미 중첩 여지** 있음 — 기존은 "프로덕션 노출 금지", 신규는 "PR base 고정" 으로 층위 다르나 독자에게 중복 인상 가능. 구분 문구 권장 |
| 3. 가독성 (PRD 문체·구조) | 9 | 테이블 중심, 4섹션 간소 PRD 구조 준수. 41줄로 분량 적정. 변경 이유/영향/롤백/검증 분리 명확. I1·I2 매트릭스가 근본 원인 즉시 파악 가능 |
| 4. 테스트 품질 (검증 체크리스트 실효성) | 7 | 8개 체크 항목 중 5개는 "특정 문자열/파일 존재" 형태로 자동 검증 가능 — 양호. 그러나 항목 5(regex 예시) 외에는 **"감지 로직이 museum-finder 실제 tasks.md 에서 동작하는지" 의 기능 회귀 테스트가 없음**. 문서 존재 ≠ 동작 증명. 회고 시나리오 재현(PR #16 상황에서 base 가 `feat/mvp-v1` 로 잡히는지 수동 검증) 항목 1줄 추가 권장 |
| 5. 보안/민감정보 | 10 | 인프라/개인정보/시크릿 어느 것도 노출 없음. 이메일·호스트·키 미포함. 참조 경로는 모두 레포 내부 상대경로 |
| 6. 성능/효율성 | 9 | rp-ship 호출당 1회 grep/read 추가 — 무시할 수준. 롤백 비용 낮음(3개 문서 revert) |
| 7. 유지보수성/범위 적절성 | 8 | 회고 개선 #1 만 반영, #2(통합 브랜치 전략 재정의)는 명시적으로 범위 밖 선언 — 적절한 스코프 컨트롤. 단 **자동 전환/실패 핸들링 기술**이 PRD 에 부족: (a) `tasks.md` 에 `통합 브랜치:` 필드가 없는 구프로젝트 대응(무해 폴백은 명시됨 OK) (b) 다중 매칭(라인 여러 개) 시 우선순위 (c) 라인이 주석/예시 내부에 있을 경우 오탐 방지 — PRD 에 한두 줄 명시 필요 |

**평균 8.57 / 최저 7 / 판정: PASS**

## 발견 이슈 (최대 10)

| # | 심각도 | 항목 |
|:-:|:-----:|------|
| M1 | Medium | **감지 regex/파싱 규칙 미확정**: PRD L20 은 "`통합 브랜치:` 또는 `feat/mvp-*` 패턴 라인 추출" 이라 기술. museum-finder tasks.md 실제 라인은 L5 `- 통합 브랜치: \`feat/mvp-v1\` · 태스크 브랜치 ...` 와 L137 주석 블록 내 `└── feat/mvp-v1`, L144 본문 설명 등 다수. **어느 라인을 선택할지 우선순위 규칙**이 스킬에 명시돼야 함. 권장: "첫 줄 = `통합 브랜치:` 접두 라인 1건 우선, 백틱 내부 브랜치명만 추출, 없으면 `CLAUDE.md` 폴백" |
| M2 | Medium | **기존 절대 규칙과 의미 중첩**: CLAUDE.md L125 "feat 브랜치 직행 배포 금지"는 "프로덕션 노출"(머지 후 서버 기동) 층위, 신규 규칙은 "PR base 선정" 층위. 독자가 중복 규칙으로 오인하지 않도록 신규 규칙 문구에 "(PR base 선정 게이트 — 프로덕션 노출 규칙과 별개)" 부기 권장 |
| M3 | Medium | **기능 회귀 검증 부재**: 검증 체크리스트는 모두 "문서에 이런 문자열이 있는가" 수준. 실제 museum-finder 에서 rp-ship 실행 시 base 가 `feat/mvp-v1` 로 잡히는지 수동/기록 증거 1건 권장 |
| L1 | Low | **`CLAUDE.md` 의 `통합 브랜치:` 필드**가 현재 museum-finder 로컬 CLAUDE.md 에 부재(확인 완료). 폴백 순서 `tasks.md → CLAUDE.md → default` 는 타당하나 **"CLAUDE.md 필드 스펙"** 이 어디에도 정의되지 않음 — 이후 신규 프로젝트가 무엇을 적어야 할지 모호. 스펙 1줄(예: `## 배포 규칙` 표에 `통합 브랜치` 행) 템플릿화 또는 범위 밖 명시 |
| L2 | Low | **`gh pr edit --base` 재타깃 사이드이펙트 미고지**: 기존 OPEN PR 의 base 를 바꾸면 GitHub 가 diff/review 상태를 재계산하고 기존 리뷰 코멘트 컨텍스트가 흐트러질 수 있음 — 사용자 공지 1줄 권장 |
| L3 | Low | **리뷰 파일명 규약 이번 범위 밖**: 회고 I5(파일명 컨벤션 혼재)는 별건 — 본 PRD 에서 "범위 밖" 명시 없음. 혼동 방지 위해 "비변경" 섹션에 한 줄 추가 권장 (하드 요구 아님) |

잔존 High/Critical: **0**

## 권고 (최대 5)

| # | 권고 |
|:-:|------|
| R1 | `docs/skills/rp-ship.md` 의 "0. PR base 결정" 서브섹션에 **정확한 파싱 규칙** 박스 추가: (a) `^\s*-?\s*통합 브랜치:\s*` 접두 매칭, (b) 백틱 또는 공백 경계 내 첫 브랜치 식별자 추출, (c) 매칭 라인 2건+이면 첫 1건 선택 + 경고 로그 |
| R2 | 검증 체크리스트에 "museum-finder 대상 드라이런: `rp-ship` 이 `feat/mvp-v1` 을 base 로 해석하는지 쉘 스니펫 실행 결과 기록" 1줄 추가 |
| R3 | CLAUDE.md 신규 절대 규칙 문구 말미에 "(PR base 선정 게이트 — 기존 `feat 브랜치 직행 배포 금지` 와 별개 층위)" 명시 |
| R4 | `docs/templates/` 에 `CLAUDE.md` 의 `통합 브랜치` 필드 예시 스니펫 추가 OR PRD "비변경" 에 "템플릿 변경은 범위 밖" 명시 — 택1 |
| R5 | `gh pr edit --base` 재타깃 시 사용자에게 "기존 리뷰 코멘트 컨텍스트가 재계산됨" 안내 메시지 출력 명세를 스킬에 포함 |

## 민감 정보 스캔 결과

PRD 및 영향 파일 범위에서 다음 검사:
- 호스트/IP/포트/계정명/인증서 경로: **미검출**
- 실명/개인 이메일/연락처/SNS ID: **미검출**
- 비밀번호/API 키/토큰/개인키: **미검출**

PRD 본문 참조 경로는 전부 레포 내부 상대경로. `museum-finder` 프로젝트명은 공개 가능한 레포 식별자 수준으로 판단. **통과**.

## 판정

**PASS** — 평균 8.57 / 최저 7 / High·Critical 0.

근거: 회고 I1·I2 명확한 문제 정의 + 제안 #1 로의 범위 축소 적절. 영향 파일 3개와 변경 계획이 실제 파일 내용(rp-ship.md 현행 "자동 수행 1~9" 구조, harness-ship.md 흐름도·CI 규칙 표, CLAUDE.md 절대 규칙 목록)과 모순 없음. museum-finder tasks.md 에 감지 대상 라인 실존 확인. 롤백 비용 낮음(문서 3개 revert). 잔존 High/Critical 없음.

남은 Medium 3건(M1 파싱 규칙·M2 규칙 중첩 고지·M3 기능 회귀 검증)은 다음 단계(구현)에서 스킬 본문 상세화로 해소 권장 — PASS 차단 사유 아님. Codex meta 리뷰에서 동일 항목이 격상될 경우 반영 필수.
