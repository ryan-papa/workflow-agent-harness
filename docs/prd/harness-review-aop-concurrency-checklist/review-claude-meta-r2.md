# Claude Meta Review r2 — 간소 PRD 재리뷰

- 실행 주체: 서브에이전트 (claude-projects 하네스 메타 변경 재리뷰 위임, r1 과 다른 인스턴스)
- 대상:
  - `docs/prd/harness-review-aop-concurrency-checklist/prd.md` (영향 파일 섹션 갱신)
  - `docs/harness-code-review.md` (1a·1b 서브섹션 + 판정 "특수 규칙" 표 보강)
- r1 결과: FAIL (평균 7.67 / 최저 6). 차단 사유 I1/I4 (판정 규칙 불명확) · I2 (Spring 편중). 권고 R3·R4·R5 동반 반영.
- 역할 경계: Claude 채점만. `/codex:review` 집행·저장은 메인 에이전트.
- 재시도 카운트: r2 (2/3).

## 1. 항목별 점수 (메타 간소 리뷰 6항목, 각 0~10)

| 항목 | 점수 | 근거 |
|------|:---:|------|
| 문서 정합성 | 9 | PRD 영향 파일 섹션이 실제 `harness-code-review.md` 변경(1a·1b 서브섹션 + 판정 "특수 규칙" 표 + 스택 독립 원칙 + `RACE-ACCEPTED` 옵션)을 한 줄 한 줄 정확히 반영. PRD 검증 체크리스트 6개 항목과 문서 실체 1:1 대응. T-22 Codex 지적 P1-01·P1-02 → 1b·1a 의 패턴 예시로 재사용, 역추적 가능. r1 이 지적한 "PRD ↔ 문서 불일치" 없음 |
| 하네스 연쇄 영향 | 9 | CLAUDE.md 트리·스킬 경로·7항목 체계·3회 재시도 규칙 모두 불변. "판정" 섹션 통과 조건 표에 `1b FAIL 조건 미적중` 행이 추가되고 **"1b 적중 시 점수와 무관하게 전체 FAIL (우회 불가)"** 문장이 조건 표 바로 아래 위치 → r1 I1 해소. 특수 규칙 표가 1a(감점형·최대 7점)/1b(FAIL형·전체 FAIL)를 이진 판정 가능하게 분리. 재시도 카운트 소비 규칙도 "1b 적중 시에도 카운트 소비" 로 명시 — 우회 경로 차단 |
| 규칙의 실효성 | 9 | ① 1b 8점 질문이 "**피호출자 기준**, 호출자 어드바이스 여부 무관" 으로 재표현 — T-22 의 `evaluateAll()` (어드바이스 없음) → `this.evaluate()` (`@Transactional`) 케이스를 grep 수준에서 이진 포착. r1 T-22 역체크의 "호출자 쪽 오판 여지" 보정 완료. ② 1a 에 "in-memory counter / quota + DB 트랜잭션" 행이 신설돼 T-22 P2-01 (recordSubmission 롤백 누수) 도 직접 타격. ③ `RACE-ACCEPTED` 옵션이 "보정 메커니즘 필수" 조건부로 명시돼 포기의 남용 방지 |
| 오·오버킬 | 8 | 1a·1b 서두의 **적용 범위 단락**이 Python (FastAPI `Depends`·Celery·`@cached_property`) · Node (Nest `@UseInterceptors`·TypeORM decorator) · Go (`sync.Mutex`·`context`) 치환 예시를 구체 명시. 1a 패턴 표 "필수 방어" 컬럼이 **"개념 · (Spring 예)"** 형태로 스택 중립화 (락 / UNIQUE+충돌 / 원자 UPDATE / 멱등성 키+outbox / afterCommit) — r1 I2·I7 해소. 다만 표의 "예시" 컬럼(카운트·insert·잔고 차감) 은 여전히 관계형 DB 전제 서술이라 NoSQL·이벤트 소싱 프로젝트에는 치환 필요. 적용 범위 단락이 일반화 원칙을 선언했으므로 허용 가능 수준 |
| 누락 | 8 | r1 지적 3건 모두 반영: R3 `RACE-ACCEPTED` 메모 + 보정 메커니즘 필수 (1a 말미), R4 `AopContext.currentProxy()` 지양 근거(테스트 이중성·AOP 컨텍스트 누수·Spring 공식 비권장, 1b 말미), R5 방어책 스택 독립 재작성. 잔여: 1a `RACE-ACCEPTED` 메모의 "보정 메커니즘 지점" 이 **PR 본문에만 기록되고 코드 주석 부재 시 리뷰 시점에 확인 어려움** — 리뷰어 가이드 한 줄("메모 검증은 PR 본문 grep 으로 대체 가능") 이 있으면 완결. Low 수준 |
| 롤백 안전 | 10 | 단일 문서 변경, 실행 코드 0, CLAUDE.md·스킬·settings 연쇄 없음. PRD 롤백 섹션이 `git revert <merge-commit>` / PR close 두 경로 명시. 검증 체크리스트에 "CLAUDE.md 트리·스킬 참조 변경 없음" 재확인 항목 포함. 이 변경이 역효과(false positive 폭증) 를 일으킬 경우 문서 1개만 원복하면 즉시 0-상태 복귀 |

**평균: (9+9+9+8+8+10)/6 = 8.83** / 최저: 8 / **판정: PASS**

> 기준: 평균 ≥ 8.0 **그리고** 최저 ≥ 7. 평균 8.83 ≥ 8.0 + 최저 8 ≥ 7 → PASS.
> r1 대비: 평균 +1.16, 최저 +2. 차단 이슈(I1/I2/I4) 완전 해소, 권고(R3·R4·R5) 전량 반영.

## 2. 발견 이슈 (r2)

| # | 심각도 | 항목 | 내용 |
|:-:|:----:|------|------|
| N1 | Low | 오·오버킬 | 1a 패턴 표 "예시" 컬럼이 관계형 DB 용어(row·INSERT·차감) 중심. NoSQL·이벤트 소싱 프로젝트 리뷰어가 "해당 패턴 아님" 으로 오판 여지. 적용 범위 단락에 "저장소 종류 무관, read-decide-write 경로가 있으면 적용" 한 줄 추가 시 완결 |
| N2 | Low | 누락 | `RACE-ACCEPTED` 메모의 "보정 메커니즘" 이 구체적으로 무엇인지(예: 주기 배치 주소·스케줄러 cron·재조회 엔드포인트) 1a 에 최소 예시 1~2개 있으면 리뷰어가 "보정 메커니즘 존재 여부" 를 이진 판정 쉬움 |
| N3 | Low | 문서 정합성 | `harness-code-review.md` 판정 섹션의 "특수 규칙" 표가 "통과 조건" 표와 **시각적으로 중복 신호** (1b FAIL 조건이 두 표에 모두 등장). 의도된 강조로 볼 수 있으나, 독자에 따라 "두 규칙이 별개" 로 오해 가능. 각주로 "통과 조건 표 3행과 특수 규칙 1b 행은 동일 규칙의 재서술" 명시하면 명확 |
| N4 | Info | 하네스 연쇄 영향 | CLAUDE.md `## Harness Engineering` 테이블 9단계 설명이 "코드 리뷰 (7항목, 최저 점수제, **Codex 추가 리뷰 1회**)" 로 유지됨. "AOP·동시성 1a·1b 특수 규칙 포함" 문구 추가는 선택. 현 상태로도 링크 경유 추적 가능하므로 차단 요소 아님 |

**신규 이슈(N1~N4) 모두 Low/Info — PASS 판정에 영향 없음. 후속 간소 보강 권장.**

## 3. r1 차단 이슈 해소 확인

| r1 이슈 | 해소 여부 | 근거 |
|:-:|:---:|------|
| I1 (판정 규칙 불명확: 1b vs 최저 ≥7 상호작용) | ✅ 해소 | 판정 섹션 통과 조건 표에 `1b FAIL 조건 미적중` 행 추가 + 표 바로 아래 "1b 적중 시 점수와 무관하게 전체 FAIL (우회 불가)" 문장 명시 |
| I2 (Spring 편중) | ✅ 해소 | 1a·1b 서두 적용 범위 단락에 Python·Node·Go 치환 예시 구체 열거 + 1a 표 방어책 "개념 · (Spring 예)" 중립화 |
| I3 → R3 (RACE-ACCEPTED 포기 메모) | ✅ 반영 | 1a 말미 "명시적 포기 메모 옵션" 문단: JavaDoc 또는 PR 본문에 `RACE-ACCEPTED: <이유> + <보정 메커니즘 지점>` + "보정 메커니즘 없는 포기 금지" |
| I4 (6점 고정 vs FAIL 표현 불일치) | ✅ 해소 | 특수 규칙 표에서 1a=감점형(최대 7점 상한)/1b=FAIL형(전체 FAIL 고정) 이진 분리 — "고정" 용어 제거 |
| I5 → R4 (AopContext.currentProxy() 지양 근거) | ✅ 반영 | 1b 말미 "테스트 이중성·AOP 컨텍스트 누수·Spring 공식 문서 비권장" 3개 근거 명시 |
| R5 (표 스택 독립화) | ✅ 반영 | 방어 컬럼 전체가 "개념 · (Spring 예)" 포맷으로 재작성 |
| I6 (r1 미통과로 체크리스트 [ ] 유지) | ✅ r2 통과로 해소 예정 | 본 r2 PASS 시 메인 에이전트가 Codex 단계로 진행 |

## 4. T-22 역체크 (보강 반영 후 재시뮬레이션)

| Codex T-22 원본 지적 | 1a·1b 해당 | 재실행 시 Claude r1 포착 가능성 (r2 기준) |
|---------------------|----------|-------------------------------------|
| P1-01 `evaluateAll()` → `this.evaluate()` self-invocation | 1b 8점 질문 | **매우 높음**. "피호출자 기준, 호출자 어드바이스 무관" 재표현으로 이진 판정. grep `this\.\w+\(` + 피호출자 `@Transactional` 스캔 조합으로 자동화 가능 수준 |
| P1-02 ip_hash 집계 후 ON/OFF 판정 레이스 | 1a 표 "집계 후 상태 전이" 1행 | **매우 높음**. 패턴명·예시·방어(락/단일 SQL UPDATE) 모두 T-22 와 직접 대응. r1 이 놓친 이유("체크리스트 부재") 구조적 해소 |
| P2-01 `recordSubmission()` 롤백 누수 (in-memory counter) | 1a 표 5행 신설 "in-memory counter / quota + DB 트랜잭션" | **높음**. 신규 행이 T-22 P2-01 를 직접 명명 — "counter 증가를 DB commit 이후로 이동(afterCommit)" 방어책까지 명시 |
| P3-01 인덱스 shape | 범위 외 (성능 6) | 대상 아님 |

**역체크 결론**: r1 이 우려한 "호출자 오판" · "타 스택 적용 모호" · "in-memory counter 누락" 모두 보강됨. T-22 회차 재실행 시 Claude r1 이 구조적으로 3건 모두 포착. **보강 완결성 확인**.

## 5. 결론

**PASS** (평균 8.83 / 최저 8).

핵심 해소 포인트:
1. **판정 규칙 이진화** — 통과 조건 표 3행 + "1b 적중 시 전체 FAIL (우회 불가)" 명문 + 특수 규칙 표로 1a 감점형/1b FAIL형 강도 차이 명확.
2. **스택 독립 원칙** — 1a·1b 적용 범위 단락에 Python·Node·Go 치환 예시 구체, 표 방어책 "개념 · (Spring 예)" 중립화.
3. **T-22 역체크 3건 모두 포착 가능** — 1b 피호출자 기준 재표현 + 1a in-memory counter 행 신설로 회차 재실행 시 r1 이 놓칠 여지 제거.

후속 권고 (비차단):
- N1·N2·N3 (Low) 동반 반영 권장 — 수용 여부는 메인 에이전트 판단.
- N4 (Info) CLAUDE.md 9단계 문구 "1a·1b 포함" 1줄 추가는 임의.

회차 처리:
- 재시도 카운트: r1 + r2 소비 (2/3).
- 다음 단계: 메인 에이전트가 `/codex:review --wait` 1회 실행 + `review-codex-meta.md` 저장 → High/Critical 반영 후 `rp-ship` 진입.
- r3 불필요 — PASS 기준 충족.
